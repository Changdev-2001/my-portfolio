# [START app_yaml]
runtime: nodejs24
entrypoint: npm start

# Add this section to cut costs
instance_class: F1
automatic_scaling:
  target_cpu_utilization: 0.6
  min_instances: 0
  max_instances: 1
  max_idle_instances: 1


handlers:
  - url: /.*
    secure: always
    script: auto
# [END app_yaml]